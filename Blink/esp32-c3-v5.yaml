# Board: Generic ESP32-C3 Board (Generic)
# Definition: definitions/boards/generic-esp32c3/manifest.yaml
esphome:
  name: esp32-c3-v3
  friendly_name: esp32-c3-v3

esp32:
  variant: esp32c3
  framework:
    type: esp-idf
logger:

# Enable Home Assistant API
api:
  encryption:
    key: !secret QMB_api

ota:
  - platform: esphome
    password: !secret QMB_ota

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: esp32-c3_v2 Fallback Hotspot
    password: "jHpjQvbr2Fcu"

# In combination with the `ap` this allows the user
# to provision wifi credentials to the device.
captive_portal:

##########################################################################
    
output:
  - platform: gpio
    pin:
      number: 3
      mode: output
    id: greenLED
  - platform: gpio
    pin:
      number: 2
      mode: output
    id: oranjeLED

# Example sensor configuration
name: blinking time
unit_of_measurement: "s"
icon: "mdi:clock"
device_class: "time"
state_class: "measurement"
accuracy_decimals: 1
filters:
  - sliding_window_moving_average:
      window_size: 15
      send_every: 15

interval:
  - interval: 1000ms
    then:
      - output.turn_on: oranjeLED
      - output.turn_off: greenLED
      - delay: 250ms
      - output.turn_off: oranjeLED
      - output.turn_on: greenLED
